# ── Configuración local con credenciales ────────────────────────────────────
db.php
.env
.php-ini

# ── Archivos subidos por usuarios (binarios, PDFs, videos, etc.) ─────────────
uploads/

# ── Videos privados — excluir binarios, conservar .htaccess y .gitkeep ───────
private_videos/*.mp4
private_videos/*.webm
private_videos/*.ogg
private_videos/*.mov
private_videos/*.avi
private_videos/*.mkv
private_videos/*.m4v

# ── Librería de tercero incluida manualmente ─────────────────────────────────
libs/

# ── Dumps / exportaciones de base de datos ──────────────────────────────────
*.sql
*.zip
*.bak

# ── Sistema operativo ────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── IDEs y editores ──────────────────────────────────────────────────────────
.vscode/
.idea/
*.sublime-workspace
